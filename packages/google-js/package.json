{
  "name": "@zerobias-org/types-google-js",
  "version": "1.0.0",
  "description": "JS types for Google services",
  "type": "module",
  "main": "dist/src/index.js",
  "types": "dist/src/index.d.ts",
  "exports": {
    ".": {
      "import": "./dist/src/index.js",
      "types": "./dist/src/index.d.ts"
    }
  },
  "scripts": {
    "clean": "rimraf dist/ .docs/ generated/",
    "generate": "npm run generate:code",
    "transpile": "tsc",
    "build": "npm run generate && npm run transpile",
    "test": "mocha --inline-diffs --reporter=list",
    "test:watch": "npm run test -- --watch --reporter=min",
    "docs:tsdoc": "typedoc --plugin typedoc-plugin-markdown --out .docs",
    "docs:combine": "concat-md --toc --decrease-title-levels --dir-name-as-title .docs > README.md",
    "docs": "npm run docs:tsdoc && npm run docs:combine",
    "version": "npm run generate:badge && git add version.svg",
    "generate:badge": "npx badge $(yq -oy e .name package.json) $(npm run -s get-version) :red @flat > version.svg",
    "generate:code": "JAVA_OPTS=\"${JAVA_OPTS} -DgoogleTypesSource=../../src\" hub-generator generate -g hub-module --additional-properties supportsES6=true -p skipHubImpl=true -i ../../node_modules/@zerobias-org/types-google/models.yml -o generated/ && ./bin/fix-generated-esm.sh generated",
    "get-version": "node -p \"require('./package.json').version\"",
    "lint": "eslint src/",
    "lint:fix": "eslint --fix src/"
  },
  "engines": {
    "node": ">=22.0.0"
  },
  "author": "team@zerobias.com",
  "license": "ISC",
  "repository": {
    "type": "git",
    "url": "git@github.com:zerobias-org/types.git",
    "directory": "packages/google-js"
  },
  "publishConfig": {
    "registry": "https://pkg.zerobias.org/"
  },
  "files": [
    "dist/src",
    "dist/generated",
    "*.md",
    "bom.json"
  ],
  "dependencies": {
    "@zerobias-org/types-google": "^1.0.0",
    "@zerobias-org/types-core-js": "^1.0.0"
  }
}
