{
  "name": "@zerobias-org/types-amazon-js",
  "version": "1.0.2",
  "description": "JS types for Amazon services",
  "type": "module",
  "main": "dist/src/index.js",
  "types": "dist/src/index.d.ts",
  "exports": {
    ".": {
      "types": "./dist/src/index.d.ts",
      "import": "./dist/src/index.js",
      "require": "./dist/src/index.js",
      "default": "./dist/src/index.js"
    }
  },
  "scripts": {
    "clean": "rimraf dist/ .docs/ generated/",
    "generate": "npm run generate:code",
    "transpile": "tsc",
    "build": "npm run generate && npm run transpile",
    "test": "mocha --inline-diffs --reporter=list test/*.ts",
    "test:watch": "npm run test -- --watch --reporter=min",
    "docs:tsdoc": "typedoc --plugin typedoc-plugin-markdown --out .docs",
    "docs:combine": "concat-md --toc --decrease-title-levels --dir-name-as-title .docs > README.md",
    "docs": "npm run docs:tsdoc && npm run docs:combine",
    "version": "npm run generate:badge && git add version.svg",
    "generate:badge": "npx badge $(yq -oy e .name package.json) $(npm run -s get-version) :red @flat > version.svg",
    "generate:code": "JAVA_OPTS=\"${JAVA_OPTS} -DamazonTypesSource=../../src\" hub-generator generate -g hub-module --additional-properties supportsES6=true -p skipHubImpl=true -i ../../node_modules/@zerobias-org/types-amazon/models.yml -o generated/ && ./bin/fix-generated-esm.sh generated",
    "get-version": "node -p \"require('./package.json').version\"",
    "lint": "eslint src/",
    "lint:fix": "eslint --fix src/"
  },
  "engines": {
    "node": ">=22.0.0"
  },
  "author": "team@zerobias.com",
  "license": "ISC",
  "repository": {
    "type": "git",
    "url": "git@github.com:zerobias-org/types.git",
    "directory": "packages/amazon-js"
  },
  "publishConfig": {
    "registry": "https://npm.pkg.github.com/"
  },
  "files": [
    "dist/src",
    "dist/generated",
    "*.md",
    "bom.json"
  ],
  "dependencies": {
    "@zerobias-org/types-amazon": "^1.0.2",
    "@zerobias-org/types-core-js": "^1.0.2"
  }
}
