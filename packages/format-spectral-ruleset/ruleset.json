{
  "functions": ["check_format"],
  "rules": {
    "valid-coretype-format": {
      "severity": "error",
      "given": [
        "$..*[?(@ != null && @.format)]"
      ],
      "then": {
        "function": "check_format"
      }
    }
  }
}
